# Project TODO

The live backlog, ordered by **dependency**. Rewritten 2026-09-17 after the NCOS kernel app
was folded out to `legacy/` — items about that app (module `validate()`, `schoolId` history,
`generated_materials`, the 36 TypeScript errors) are **retired**, not pending. Nothing in
`src/` imports `legacy/`. (`storage.rules` came back on its own account in P3-3: the document
library stores members' generated files in Cloud Storage, and that is a rule file the portal
now really has — see `docs/security.md`.)

> **Anchor:** the portal must produce at least what the books produce, and must not serve
> curriculum it cannot source. Everything below is scored against that.

**Verify state at any time with:**

```bash
make check     # lint + tests + validate-curriculum + inventory + production build
make audit     # dataset vs portal agreement -> data/inventory.json (warnings are honest)
yarn test      # vitest: src/lib, buildTree, and contracts over public/curriculum
```

`make check` is also run by CI (`.github/workflows/ci.yml`) on every push, which
additionally fails if `data/inventory.json` is stale.

---

## P0 — the portal cannot run for real users yet

| # | Item | Why | Size |
|---|------|-----|------|
| P0-1 | **Configure Firebase**: copy `.env.example` → `.env.local` with the six `VITE_FIREBASE_*` values, and set the same on Vercel (Production + Preview) — *the values are the user's to supply; the pipeline around them is done (2026-09-19)* | Vite embeds these at build time, so without them the build is green and every Firebase call fails at runtime. **The build now says so**: `vite.config.js` warns loudly when any of the six is missing, writes `dist/build-info.json` (`firebaseConfigured`, `missingEnv`, `projectId`, `bundleHash`, `commit`), and `make deploy-check URL=…` fetches it plus `/`, `/curriculum/grades.json`, `/curriculum/schedules/…` and `/sw.js` — verified end to end against `yarn preview`. Locally: `cp .env.example .env.local`, fill from Firebase console → Project settings → Your apps, restart the dev server. **Updated 2026-09-20**: the same checks now run from anywhere Node does — `node scripts/verify_deploy.mjs -Url <deploy>` (`make preflight URL=…`, or `.\scripts\deploy_check.ps1` on Windows) — and it also checks the local half before anything is deployed: `.env.local` complete, and a build that actually carries the six values. `src/deployCheck.test.js` holds the Node and Python implementations to the same check list so they cannot drift. What is left is the Vercel side: six variables × Production + Preview **Updated 2026-09-20 (first real deploy)**: the production domain now serves this branch (`GET /build-info.json` → commit `6e4164f`; the Production-Branch switch worked) but **that build had none of the six values** (`firebaseConfigured: false`, all six in `missingEnv`), so the site shows the setup notice. The remaining step is Vercel → Settings → Environment Variables for **Production and Preview**, then *Deployments → Redeploy* — a variable change does not rebuild by itself. `src/lib/setupGuidance.js` now splits the notice's instructions into the laptop path and the deployment path (with the missing names, the redeploy step and `/build-info.json`), so the screen no longer tells a visitor to copy a file that does not exist on the server. **Updated 2026-09-21 (resolved)**: the values now live in the committed `src/firebaseConfig.js`, and `src/firebase.js` falls back to them for anything the build environment does not supply — so no dashboard state, encoding or cache can leave a deploy without config. Two silent traps were found and are guarded by tests, both of which the first two deploys hit: a BOM from PowerShell's `Set-Content -Encoding utf8` hides the first line of an env file from Vite's parser (the committed `.env.production` lost its API key that way, invisibly, because `.env.local` supplied it locally), and a blank `VITE_FIREBASE_*` in the environment shadows a file value because Vite lets `process.env` win. `.env.production` was removed as redundant; Vercel variables are now optional overrides, and the build log plus `configSource` in `/build-info.json` report which source a build used. **Updated 2026-09-21 (sandbox audit)**: with the config committed, an absent `.env.local` is the normal case, so the pre-flight treats it that way — both implementations fall back to the committed `src/firebaseConfig.js` for the project id, which keeps the `.firebaserc` cross-check running on a fresh clone (exactly where a wrong pin hides), and the Python local exit now prints the notes it collects (its cross-check answer used to be invisible on a clean run). Before this the two had disagreed on the absent-file case — Node failed the whole run, Python skipped the cross-check silently; `src/deployCheck.test.js` holds both to it | S |
| P0-2 | **Deploy rules + indexes**: `make deploy-rules` (or paste `firestore.rules` into the console), plus **`make deploy-storage` for the new `storage.rules`** (P3-3). `.firebaserc` is committed and pins `beacon-edu-consult-proj` | **Partly done** — the user published the ruleset once and reported it working, but the console copy predates three things now in this repo: the visibility-gated reads for `/notes/`, `/lesson_plans/` and `/weekly_forecasts/` (P2-4/P2-7, the list-query fix in `08b19ab`), the `generated_documents` block (P3-3), and Storage rules, which have never been deployed at all. `docs/verification.md` lists all three with the reason. **Updated 2026-09-20**: the console also predates the `matches()` fix (`02b166f`) that denied every library save, and `src/firestoreIndexes.test.js` now derives the composite index of every list query in `src/pages/` from the source and fails if `firestore.indexes.json` lacks it — that class of bug works in dev, works in the emulator, and in production shows an empty list with `failed-precondition`. Publishing is one command, after the new build is live; `docs/verification.md` now states the order and why it is that order. **Updated 2026-09-20 (project confirmed)**: the live Firebase project is **`beacon-edu-consult-proj`** — the app's `.env.local` names it — so `.firebaserc` was repointed from the retired `beacon-educational-consu-8005e`, and both pre-flight scripts now fail if the CLI's project and the app's ever differ again (a mismatch publishes the rules to a project the app never talks to). **If the live project is new, its database is in test mode**: open to anyone with the project id until it expires. Publish the rules today | The console is the enforcement; a committed-but-unpublished rule does nothing | S |
| P0-2b | **Storage is off for now — not a gap** | Cloud Storage for Firebase requires the Blaze plan (a linked billing account) since October 2024, and the project is on Spark. Skipping it costs exactly one feature (the document library, P3-3); everything else — curriculum, planners, exam papers, decks, all exports — is unaffected. **Updated 2026-09-20**: the app now detects the missing bucket (`probeStorage`) instead of failing with `storage/unknown` — the Save button is replaced by one honest line, the library page explains itself, and enabling Storage later needs no code change. Skip `make deploy-storage` until then | Budget, not a defect | — |
| P0-3b | **Bootstrap the first admin on the new project** (`beacon-edu-consult-proj`): enable Email/Password in Authentication, sign up once, then set `role:'admin'` + `status:'approved'` on that `users/{uid}` doc | The project is new, so it starts with no users at all — and sign-up forces `status:'pending'`/`role:'member'`, which no client is allowed to change. Until this row is done, an approved member cannot exist, so nothing past the curriculum can be driven. **Updated 2026-09-20**: `docs/build-deploy.md` now carries the whole console walkthrough (project → database → auth → storage → web app → rules → first admin) with the error each missing step causes | S |
| P0-3 | ~~**Bootstrap the first admin**: create an account, then set `role: 'admin'` (and `status: 'approved'`) on its `users/{uid}` doc in the Firebase console~~ — **done 2026-09-19** | **Done** — confirmed by the user. This is the only way in: sign-up forces `status: 'pending'`/`role: 'member'`, self-edits cannot change `role`/`status`, and only an admin can approve — so the console is the bootstrap path, and it must be repeated for each new school's first admin | S |
| P0-4 | **Drive the real flows once deployed** (the code bible's Phase 7): sign up → approve → create → view → edit → delete for a scheme, a plan, a question, and a note; check offline behavior with `yarn build && yarn preview` — *the checklist and its automated half are done (2026-09-19)*: `docs/verification.md` is the 27-step script, written to be run by an ordinary member rather than an admin, with each step's "why" (which rule, index or cache it exercises); `scripts/verify_deploy.py` covers what a machine can check. What remains is the human pass on the live project, which needs the Firebase config (P0-1). **Updated 2026-09-20**: the checklist gained a **first-deploy order** (Vercel variables → deploy → rules + indexes → first admin, with the reason the last two come last), a *When a row fails* table keyed on the symptom — `permission-denied` vs `failed-precondition` vs the setup notice vs empty dropdowns — and the rows it never had: the **starter-bank import** (17a) and the **exam paper** (19a–19e). `DataError` now shows Firestore's one-click index link, which is the whole repair for a `failed-precondition` | A green build proves nothing about rules, indexes, or the service worker — and admins pass branches ordinary members do not, which is exactly how the un-filtered-list bug hid for so long | M |

## P1 — data integrity (what teachers actually read)

| # | Item | Why | Size |
|---|------|-----|------|
| P1-1 | ~~**Cross-check the 8 unaudited subject-grades against their NaCCA PDFs**, then promote them into `data/curriculum/` and add their counts to the `EXPECTED` table in `scripts/audit/audit_a_databases.py`~~ — **done 2026-09-18**: all 8 pass Audit B, their text fields are cleaned, and `scripts/promote_reference_subjects.py` moved them (and `english-language B5`) into `data/curriculum/` with counts-bearing summaries and rows in `EXPECTED`. Audit A now covers all 84 subject-grades | The portal served curriculum for 8 subject-grades (~714 indicators) that had never been checked against its source. It was real NaCCA curriculum with unverified extraction; the whole arc is in the progress notes below | M |
| P1-9 | ~~**`english-language B5` is audited but not reproducible**~~ — **done 2026-09-18**: promoted into `data/curriculum/`, where Audit A enumerates it and its `EXPECTED` row (133) holds. Audit A now reports 84 files / 84 PASS and reproduces every row it prints | Audit A's committed row could not be re-derived by anyone | S |
| P1-10 | ~~**`B7.4.2.3.1` is a fabricated record in `data/reference/french_B7_...`**~~ — **done 2026-09-19**: `scripts/drop_front_matter_records.py` (report by default, `--apply` deletes) found **four** records built from a print's front-matter notation example, not one — french B7 `B7.4.2.3.1` (`french_CCP_B7-B9.pdf` p29, where the print writes `B7/JHS1 .4.2.3.1`, so the year annotation hides the code from a plain search; docs said "p. xxix" and that was wrong), ghanaian-language B7 `B7.4.2.3.1` (p30), rme B7 `B7.2.3.4.5` (p28) and science B4 `B4.2.4.1.2` (p18). All four were reference-layer copies — no served database ever carried them — and are deleted, with the deleted records kept verbatim in `data/audit/front_matter_records.json` (a test pins both the deletion and that the served copy never had the code). The same scan separates the **302 label-form records the prints do carry** (74 served — see P1-11 — plus 228 in `data/reference/`) as fill-me, not drop-me | A record the curriculum does not have, built from the front matter's worked example. The lesson is in the shape: *label-form* alone proves nothing (202 other reference records share it and the prints carry their text), so dropping needs the print's own evidence — one occurrence, on an `Example:`/`ANNOTATION` page, with no body row | S |
| P1-11 | ~~**74 served records are label-form placeholders**~~ — **done 2026-09-19**: `scripts/fill_label_form_text.py` (report by default, `--apply` writes) read the print cell by cell and filled **793 fields in 269 records across 21 files** — the whole of `owop_B4`/`B5`/`B6` (74 records: 74 `ind_desc`, 73 `cs_desc`, 74 `strand`, 74 `sub_strand`) plus the inert reference copies of the same subject-grades and the older reference extractions of nine other subjects. Every write is cell-verified: the reading must begin with the record's own code, say at least three words, and not be a label; the content standard must come from a cell beginning with the record's own `cs_code`; a strand/sub-strand name must be the print's heading whose number matches the code's component. All of it is in `data/audit/label_form_text.json`. The three `owop_B4`–`B6` summaries gained the strand names the databases now carry, and the served bundle no longer shows the shape anywhere — Audit A prints `placeholder=0` for all 84 subject-grades (it printed 74 before). **Two fields are deliberately left**: `owop_B6.4.4.1.1`'s content standard, whose print row sets `B6.4.6.1` beside the indicator (another standard's code), and 97 more in the reference layer whose cells do not verify | Teachers read the code restated instead of the curriculum, and generated documents inherit it. This was a **filling** job: `owop_B4-B6.pdf` carries the real text (pp. 18/40/61) | M |
| P1-13 | ~~**Nothing assembles a paper**~~ — **done 2026-09-19**: `/portal/questions/exam` (`ExamBuilder.jsx`) builds one from two pools at once — the member's own `questions` and the served starter bank for the subject-grade, so the B7–B9 questions are usable in a paper without importing them — through `src/lib/examPaper.js` (pure, 14 tests). The paper stops before it would overshoot the target marks, prints the shortfall instead of rounding up, and **names a section it had to leave out** rather than quietly making the paper bigger than it said; coverage comes before depth (every indicator's first question before any indicator's second); a dropped question recomposes the paper live. Two exports: student copy, teacher copy with the marking scheme (offered to My library). Nothing is written to Firestore — the paper is the PDF. **Term-scoped (same day)**: *Which work does it cover?* restricts the pool to the indicators the chosen term schedules (`termScope`, read from `public/curriculum/schedules/`), so an end-of-term paper covers the term's work and not the next one's, and the page prints what the scope leaves untested — *"Term 2 schedules 56 indicators — the pool asks about 41 (73%). 15 have no question: 5 in Patterns and Relations, …"* — which is what sent the next round of rules (`jhs-sequences`, `jhs-table-of-values`, `jhs-algebra-simplify`, `jhs-algebra-expand`, `jhs-gradient`) and lifted B7's term-1 coverage from 57% to 70%. **Completed 2026-09-20**: the authored JHS bank (see P1-5) covers every indicator those rules could not reach, so the same line now reads 100% for every JHS term and the paper's Section C — empty until then, because the bank held no `essay` items — prints. `src/sidebarRoutes.test.js` is new with it: every sidebar link must name a route the router declares. **Updated 2026-09-20 (the paper reaches its length)**: sections are budgeted so their shares add up to the target exactly (rounded one by one, a 50-mark paper came out at 51), and the marks a section cannot spend — because the pool holds none of its types, or only questions longer than its share — go to the sections that can still print, so a primary paper with no essays is 50 marks of Sections A and B instead of 38 marks with a shortfall line; the omitted section is still named, now with its reason (`too-long` or `no-candidates`) and the advice that fits it | The bank could print a selection; nothing could make one, so the paper was only as good as the teacher's patience in ticking 50 boxes | M |
| P1-12 | ~~**3,639 served indicators name their sub-strand by its code**~~ — **done 2026-09-19**: `scripts/fix_sub_strand_names.py` (report by default, `--apply` writes) read the print's own block heading for every code-shaped sub-strand and named **3,665 of the 3,679** records across 73 databases — from the record's own row (3,612), its content standard (43) and the same block read elsewhere in the print (10) — plus the **11,652 lesson rows** in `data/lessons/` that repeated the code, because the scheme of learning prints the lesson's own `sub_strand` (the served schedules went from 11,700 code-shaped rows to 48). The reader is P1-11's, widened: kindergarten and social studies are registered in `fix_ind_desc_exemplars.PRINTS`, the heading forms cover `SUB - STRAND` / `SUB STRAND` / a number the print left out / name-first / a heading carrying its own code, and a name is written only when it comes from the print's heading, is de-kerned against that print's vocabulary, is not a code / label / column heading / dot-leader line, appears again on the heading's page, and is the block's only reading. **The first pass carried a folding bug worth remembering**: the rme CCP sets `B7/JHS1 2.2.1.1` with no dot after the class, and a fold that removes the whitespace first lets the marker's `\d*` eat the strand digit — the code landed one component short, which refused 18 rme blocks as "no heading" *and* named 41 rme records from the neighbouring block. The class marker is now stripped before the space is collapsed and the 41 are corrected. **14 refusals** stand, all blocks the print itself names two ways — 12 rows (kindergarten `PLANTS- 1`/`PLANTS- 2`, `MY NATIONAL AND CIVIC VALUES`/`OUR BELIEFS`, mathematics B4/B5/B8/B9, ghanaian-language B4, social-studies B7) and 2 mathematics B9 content standards. The 31 recurring kerned readings (`Appreciati n g`, `Organi s ation`, `GENE RATION`, `samaison`) are repaired from a curated table, each verified against that print's vocabulary before it applies — the computing print's own `TECHNOLOGY INTHE COMMUNITY` is left verbatim. Everything read, refused and repaired is in `data/audit/sub_strand_names.json`; the served bundle keeps a code in only those 14 indicators, their 48 schedule rows and 3 scheme cells, and a test pins the fill, the refusal set and that nothing else carries one | The browser, the scheme of learning and the lesson plan showed a code where the print has a name (`Sub-Strand 4: Farming in Ghana`) | M |
| P1-2 | ~~**Fill the L1 provenance gaps**~~ — **done 2026-09-18**: `english-language B4`'s summary was in `data/reference/` and was promoted; `mathematics B1` had none and now has `math_curriculum_summary.json`, its `sourceTitle`/`sourceUrl` taken from the B2/B3 summary of the same document. (`science B1` already had one — the row was stale.) Every one of the 84 L1 pairs now has a summary with a `counts` block | 184 indicators had unverifiable provenance | S |
| P1-3 | ~~**Repair the 8 malformed reference summaries** (no `counts` block)~~ — **done 2026-09-18**: the eight were the promoted subject-grades, and their summaries now carry `counts` + a per-strand breakdown derived from their databases, with the vestigial `sourceVerified` flag dropped (nothing read it, and it said `false` about subject-grades Audit B passes) | Any code that reads `summary['counts']` raised on them | S |
| P1-4 | ~~**Say what L2 is, then act on it**~~ — **done 2026-09-19, the honest-labelling route**: `scripts/audit/audit_d_l2_template.py` (`make audit-l2`) measures each field inside one subject-grade and writes `data/audit/l2_template.json` — across the 13,140 slots `rpk` is one value in **70 of 73** subject-grades, `plenary` in 60, `assessment`/`competencies` in 67, `resources`/`keywords` in 66, while `starter` (median 156 distinct of 180) and `main` (60) are content. `src/lib/lessonTemplate.js` repeats that measurement from the loaded schedule; a plan filled from the curriculum records the sections it inherited (`inheritedFields`, `prefilledFrom`), the form says so in a sentence, and both exports mark exactly those headings `(teaching template)` with a footnote — starter/main are never marked, because they are the lesson's own. **Not enriched**: 13,140 hand-made assessments is authoring work with a teaching budget behind it, not a cleanup, and the documents are honest either way now. `make check` fails if the app's routine list drifts from the measurement | Marketing and authoring budget depend on this | S |
| P1-5 | ~~**Question bank content**: zero questions exist in the bundle~~ — **done 2026-09-19**: the bundle now carries **378 questions for 407 mathematics indicators B2–B9 (186 covered, 46%)**, generated from rules and validated against the served indicators. `scripts/generate_question_bank.py` (report by default, `--apply` writes `data/questions/<subject>/<grade>.generated.json`) states each rule's numbers in the prompt and computes the answer, follows the indicator's own number range, and only fires where the wording matches — so an unmatched indicator gets no questions rather than a padded one. `scripts/build_question_bank.py` merges the authored file (which the generator never touches) with the generated one, refuses orphans/an MCQ without its answer/repeated options/missing prompt or marks/duplicate ids, and writes `public/curriculum/questions/` + an index with the coverage. `scripts/validate_app_curriculum.py` re-checks the committed bank, so `make check` catches drift. In the app, `QuestionBank.jsx` has a **Starter bank** panel that imports a subject-grade into Firestore (`src/lib/starterBank.js`; 8 tests) so imported questions behave like hand-written ones, with `source`/`starterBankId` keeping the provenance. **Updated 2026-09-19 (JHS bank)**: the bank now serves **378 questions over 407 indicators of mathematics B2–B9** (186 covered, 46%) — B7 102, B8 67, B9 72 — because the generator grew a second rule band written for JHS wording (`jhs-index-form`, `jhs-laws-of-indices`, `jhs-pythagoras`, `jhs-probability`, `jhs-vectors-bearings`, `jhs-surface-area`, `jhs-sequences`, `jhs-table-of-values`, `jhs-algebra-simplify`, `jhs-algebra-expand`, `jhs-gradient`, …), with the primary rules carrying `veto`es so they cannot fire on an algebra or geometry indicator. Two misfires found while writing it are now regression-guarded: rules match `ind_desc` only (matching `cs_desc` asked a data-collection indicator what five pens cost — "cost" inside "…taking into consideration…"), and the money and central-tendency patterns hold word boundaries ("cedi" inside "preceding", "mode" inside "Model"). `make check` now runs `generate_question_bank.py --verify`, which fails if the committed files differ from what the rules produce. **Updated 2026-09-20 (authored JHS bank)**: the rules can never answer every indicator — a lesson can ask a pupil to *construct a perpendicular bisector*, *draw a locus* or *simplify a surd*, and no rule can compute that answer — so `data/questions/mathematics/B{7,8,9}.json` now hold **148 hand-written items** covering exactly the indicators the rules leave empty. The bank serves **560 questions, 249 of 407 indicators (61%)**, and the JHS years are **complete: B7 67/67, B8 49/49, B9 43/43 — 100% of every term's schedule**, so a term paper for any of the three years has a question for every indicator it schedules. The construction lessons are `essay` items ("construct … and state …") with the steps in the marking scheme, which is also what fills the paper's Section C: before this the bank had only mcq and short items, so every composed paper omitted Section C with "no question of the types this section prints". Authored items carry `source: "authored"`; the merge refuses an id clash rather than overwriting. **Updated 2026-09-20 (primary bank complete)**: three more rule passes over primary wording (`nonstandard-units`, `shape-from-attributes`, `movement-length`, `opposite-values`, `commutativity`, `unit-fraction`, `quadrilateral-types`, `measure-referents`, `data-method`, `table-from-problem`, `place-value-parts`, `integer-stories`, `expression-phrases`, `problem-to-equation`, `equation-meaning`, `equation-from-picture`, `reflection-coordinates`, `likelihood`, `graph-attributes`, `graph-from-table`, …) closed B2–B6 to 100%, with six hand-written items for the last five indicators no rule could answer (compare two graphs of the same data, represent a pattern visually, conduct a probability experiment, design a questionnaire), plus the long-answer items the primary
pools had none of (B2 3, B3 4, B5 1) so Section C prints on a primary paper too. The bank now
serves **1,081 questions over all 407 mathematics indicators B2–B9 — 100% — and 100% of every term's schedule**: B2 79 (29/29), B3 96 (39/39), B4 187 (71/71), B5 174 (67/67), B6 115 (42/42), B7 176, B8 131, B9 115. One finding worth keeping: `indicator_text()` lower-cases the description, so a rule pattern written with a capital letter never fires — the metric rule's `Kilogram` branch had been dead since it was written (now in `gotchas.md`, with the rule report and the "still left" list as the guard) | The generators rendered an empty selection from an empty bank. **Deliberately partial**: only mathematics, because every rule has to be able to compute its own answer — indicators a rule cannot reach now want an author, and only `mathematics` has a generator at all | L |
| P1-6 | ~~**Clean the L2 `ind_desc` records with a repeated trailing sentence**~~ — **done 2026-09-18**: `scripts/fix_ind_desc_exemplars.py` cut the print's exemplar tail off **253 records in 8 databases** and out of **1,137 lesson slots in 17 lesson files** (`ind_desc`/`perf_indicator` 1,137 each, `starter`/`main` 962 each), every cut verified against the row the print sets and recorded in `data/audit/ind_desc_exemplars.json`. Correcting the reader's column band (the career-technology print draws its content-standard edge at x≈59, which a 60 floor dropped, so the reader fell back to a window that swallowed the core-competencies column) exposed 11 more cuts and the **27 records whose `ind_desc` held the neighbouring column** — including two computing B9 records that held nothing else and now carry the indicator restored from the print's own row **Residuals re-measured 2026-09-19**: the ten creative-arts B5/B6 "mismatches" were a reader bug, not a fidelity finding — the verifier compared against whichever row sat on the page and could not see the print's de-kerning (`L earners`). Anchoring the read on the record's own code and searching a de-kerned copy (with the earliest marker winning) resolved **twelve creative-arts records and one career-technology record**, whose dangling `Learners are to` tails are now cut with the print's row confirming them verbatim (+1 verified, −0). What is left is reported, not cut: **149** records whose tail is real exemplar content the indicator does not carry (cutting would delete a sentence no other field holds), **20** the print does not back (rows whose code prints in a form the reader does not match, one kindergarten print that is not registered), and the four row-ambiguous records below. Left reported, not cut: **149 records** whose tail is real exemplar content the indicator does not carry (career-technology's numbered steps, history's `Enquiry route:` blocks), and four records where the reader cannot confirm the row (creative-arts B7–B9 `…1.3.1.1`, mathematics B3 `B3.1.2.5.1`) | Visible artefact in every generated document | M |
| P1-7 | ~~**`indicatorDocId` keys on the bare indicator code**~~ — **done 2026-09-18**: the helper moved to `src/lib/docIds.js` and now takes the subject as well (`uid_subject_code`); `useCollection.js` re-exports it so the old import path keeps working. Nothing had adopted it yet, so no ids were in the wild; a test pins that the same code in two subjects no longer collides. **Re-verified 2026-09-21**: still zero adopters — the plan and question forms write with `addDoc` auto-ids and the starter-bank import carries provenance in a `starterBankId` field — so the helper is a deliberate guardrail for the day a deterministic per-author-per-indicator key is wanted; using it is convention, not an enforced invariant | A `B4.1.1.1.1` plan belonged to whichever B4 subject wrote first | S |
| P1-8 | ~~**The book skeleton seeds only the first session of each indicator**~~ — **done 2026-09-18**: `seed/build_book_skeleton.py` names the session it drafted from ("Drafted from the first scheduled session (Term 1 · week 1 · Monday)") and prints every later session whose own `main` differs as a **Later sessions** box, with that session's schedule label and its activity text verbatim; sessions that only repeat the first one are folded into one italic line instead. Verified on creative-arts B4: 47 lessons, 47 with later sessions, 3 sessions each, none dropped. The skeleton no longer promises more than it seeds — and it no longer drops teaching content the database holds | 134 of 263 multi-session indicators held different main-activity text | M |

### P1-1 progress — kindergarten KG1/KG2 (2026-09-18)

The KG source PDF arrived (`data/sources/kindergarten_KG1-KG2.pdf`, 231pp, genuine NaCCA
*Kindergarten Curriculum (KG 1&2)*, September 2019). Checking it turned up the same class of
fault as computing/french, plus one of its own:

- **strand names were placeholders** — all 339 indicators read `"Strand 1"`…`"Strand 7"`. The
  curriculum is themed, and the PDF names each one: ALL ABOUT ME, MY FAMILY, VALUES AND
  BELIEFS, MY LOCAL COMMUNITY, MY NATION GHANA, ALL AROUND US, MY GLOBAL COMMUNITY — the same
  seven for both grades. `fix_reference_structure.py` now covers kindergarten and wrote them.
- **one indicator was a stub** — `K1.1.1.1.1`, the *first* KG1 indicator a teacher meets, had
  `ind_desc: "Kindergarten Learning Indicator K1.1.1.1.1"`: the code restated, no content. The
  script now refills descriptions that are stubs from the PDF text, and did.

**Codes match the source**: K1 169/169 exactly; K2 170/170 with one extra code in the PDF
(`K2.1.1.1.51`) that is a **misprint inside an exemplar cross-reference** — the same reference is
printed correctly as `K2.1.1.1.5` on the following line. Reported by the audit as informational.

Finding the KG strand names needed a rule the earlier pass did not have: the print labels its
strand-7 heading `SUB STRAND 7: MY GLOBAL COMMUNITY` (p135), while `SUB STRAND 7: GARDENING`
(p125) really is a sub-strand. The scan now reads a sub-strand heading as a strand heading only
when its number agrees with the codes beneath it *and* no strand heading for that number has been
seen yet.

**Then the same field gaps as computing/french** — `keywords` empty on all 339 and descriptions
carrying the PDF's footer text — plus the KG content standards; all three were closed on
2026-09-18 by `scripts/fix_reference_text.py`, and the two databases were promoted into
`data/curriculum/` the same day (see the promotion note below).

### P1-1 progress — computing + french B4–B6 (2026-09-18)

The two source PDFs arrived and were checked against the served data. **Both are genuine NaCCA
documents** (Computing 76pp, French 123pp, September 2019) and they now sit in `data/sources/`
as `computing_B4-B6.pdf` / `french_B4-B6.pdf`, giving both subjects B4–B9 source coverage
alongside the existing `*_CCP_B7-B9.pdf` files.

**The indicator sets match the source exactly** — computing 27/81/98 and french 88/90/89 for
B4/B5/B6, with zero codes missing and zero invented. That is the question the audit could not
answer before, and it is now answered: the *content* of these six subject-grades is complete and
correctly coded.

**What was wrong was the hierarchy labels.** Two distinct faults, both fixed by
`scripts/fix_reference_structure.py` (which re-reads the strand headings from the PDFs):

| | before | after |
|---|---|---|
| computing B4 | `strand: "Strand 1"` (placeholder) | `"1. WORD PROCESSING"` |
| french B4 | `strand: "Saluer et prendre congé"` — a **sub-strand** name, one level too low | `"1. L'IDENTITÉ"` |

473 indicators changed, and the change is exactly the two fields (verified: indicator key sets
identical, only `strand`/`sub_strand` differ). The strand names now match each PDF's table of
contents. `sub_strand` became `"Sub-strand B4.1.1"` — the generic form every audited subject
already uses, so the browse tree looks the same whichever copy a subject comes from. The real
sub-strand names are in the PDFs if the app ever wants to display them.

**Audited.** These six now pass Audit B (below), so `verified` is true for them and the app no
longer flags them. What remains is field completeness, not authenticity:

**The field gaps are closed** (2026-09-18). All three were settled against the prints in
`data/sources/`, never by rewriting:

- ~~**french: content standard empty** for 85/88 (B4), 89/90 (B5), 88/89 (B6)~~ — **done
  2026-09-18**, see the content-standards note below. All 266 records now carry the skill area
  the print names as the standard, and the eleven french B7–B9 standards that had the
  neighbouring column glued in front of them were repaired in the same pass (36 records).
- ~~**keywords empty** for all 473 (and for KG1/KG2, 169/170)~~ — **done 2026-09-18**: all 812
  records of the eight subject-grades carry the audited convention's tag
  (`computing, b5, upper-primary`, `kindergarten, kg1, kindergarten`). The six drifted reference
  copies of audited subjects (creative-arts B4–B6, social-studies B7–B9) were stamped with the
  tag their curriculum copy carries — their *records* are still a stale extraction and stay out
  of scope.
- ~~**Neighbouring columns and page footers bleed into indicator text**~~ — **done 2026-09-18**:
  397 `ind_desc` values were cleaned. The furniture was the page footer (118 records, e.g.
  `"… desktop 3 © NaCCA, Ministry of Education 2019"`), the reference codes (`LL2`, `(CC)`,
  `N3.1` — 166), the CORE COMPETENCIES list of the column next door (54, incl. the few that
  *opened* with it), the table's headings, the row markers the extractor read past, and one cell
  label. 383 of the 397 survivors are verbatim readings of the record's own row and 14 are
  word-by-word (the extraction braided the columns); nothing the print does not carry was
  written, and none is left over.
- ~~**KG1–KG2 content standards**~~ — **done 2026-09-18**: 13 records were filled (the print's
  standard column had been missed for `K1.3.1.1` and `K2.1.3.1`), 7 were completed, 21 trimmed of
  the heading next door, and 5 (`K2.5.1.1`) replaced — they held indicator-column text, and the
  displaced values are kept in `data/audit/reference_text_fixes.json`. `K1.3.2.1`'s cell is
  **blank in the print**, so its five records stay empty, and the audit's
  "the print has no sentence" verdict for it stands as a print defect.

The work and its trail are `scripts/fix_reference_text.py` (report by default, `--apply` writes)
and `data/audit/reference_text_fixes.json`; the rule and the residual check are in
`docs/curriculum-data.md`.

### P1-1 — the promotion (2026-09-18)

`scripts/promote_reference_subjects.py` (report by default, `--apply` moves files) finished the
item. Eighteen files moved into `data/curriculum/` — the databases and summaries of computing
B4–B6, french B4–B6 and kindergarten KG1/KG2 — plus `english-language B5` (the database, and the
summary that was already there) and `english-language B4`'s summary. Two summaries were written
from scratch: `english-language B4`'s (its file *was* the reference copy, which moved) and
`mathematics B1`'s, whose provenance came from the B2/B3 summary of the same document.

Three things had to be true for the move to be honest, and each was checked before the files went:

* **Audit A had to be able to audit them.** It enumerated `_B4_`-shaped file names and validated
  `^B(\d)\.` codes, so kindergarten's `_KG1_`/`K1.3.2.1.4` shape was invisible to it. It now
  enumerates `_KG1_`, accepts the `K`/`KG` mismatch in the grade column, and keeps the file-name
  form as the grade. Its `EXPECTED` table gained the rows: computing `B4 27 B5 81 B6 98`, french
  `B4 88 B5 90 B6 89`, kindergarten `KG1 169 KG2 170`.
* **The one empty field the move would expose had to be filled.** `computing B5.6.4.9.1` had no
  `cs_desc` although the print prints `Demonstrate proficiency in Digital Literacy.` on p48 — the
  computing print's own cell code reads `B5.6.4.9.1.` (a three-part standard keyed as
  `B5.6.4.9`), which the earlier scan could not match. `fix_reference_text.py` now reads the
  print's content-standard column for computing too, fills empty fields from it, and reports
  standards whose cell is blank. French B6's `B6.1.2.5.3` and kindergarten's five `K1.3.2.1`
  records are the two that stay empty, because the print's own cell is blank — now carried as
  named exemptions in Audit A's `BLANK_IN_PRINT` and printed as `blank-in-print=N`, so the audit
  still fails on every *other* empty field.
* **The tooling had to stop assuming the old layout.** `fix_reference_structure.py`,
  `fix_french_content_standards.py` and `fix_reference_text.py` now resolve their files through
  `find_data` instead of joining onto `data/reference/`, so they follow the served copy. All three
  re-run clean: 0 indicators would change.

Result: **Audit A 84 files / 84 PASS / 4,040 indicators** (it printed 75 PASS before), Audit B
unchanged at 83 PASS + 1 WARN, and the inventory's L1 reports 84 pairs / 84 summaries, its
"8 app-bundle subject-grades exist ONLY in `data/reference/`" warning gone. The bundle stamps
`source: 'curriculum'` for all 84, and the test that used to pin nine fallback-served
subject-grades now pins that *nothing* is served from the fallback.

What stays in `data/reference/`: the older extraction of the same subject-grades
(`english-language_B5_curriculum_db.json`, `english-language_B[2-6]_curriculum_db.json`), the
drifted creative-arts B4–B6 and social-studies B7–B9 copies (P1-6 territory; the four records the
front matter fabricated were deleted from these copies on 2026-09-19, P1-10, and the 228 records
that restate their code were filled on 2026-09-19, P1-11) and the french
B7–B9 copies, which is where `fix_french_content_standards.py` keeps its repair of the
neighbour-column bleed. They are inert: `find_data` prefers `data/curriculum/`, and nothing served
comes from there any more.

### P1-1 progress — the french content standards (2026-09-18)

`cs_desc` was empty for 262 of the 267 french B4–B6 records, and the five that were filled had
been harvested from *indicator* rows. The reason is not a lost field: **the french B4–B6 print
has no sentence-style content standards.** Its CONTENT STANDARDS column holds one of four skill
areas, and the SCOPE AND SEQUENCE table (pp. xviii–xx) lists exactly those four — in that order —
for every one of the document's 18 sub-strands. The front matter (p. xvii) defines the code, so
the fourth component *is* the content-standard number and therefore the skill:

```
B4.1.2.3.1   B4 = class   1 = strand   2 = sub-strand   3 = content standard   1 = indicator
                                                        ^ 1 CO  2 PO  3 CE  4 PE
```

`scripts/fix_french_content_standards.py` (report by default, `--apply` writes) cross-checks both
claims against the PDF before writing — 18 sub-strand blocks listing the four skills in order, and
240 CS cells in the body carrying those same four labels and nothing else — then writes the label.
**266 records filled**, one per line, nothing else touched. The five values it replaced, and the
rule itself, are recorded in `data/audit/french_content_standards.json`.

**The same pass repaired french B7–B9**, where the defect was the mirror image: eleven content
standards (37 records) in `data/reference/` carried the CORE COMPETENCIES column *in front of* the
statement, or a placeholder (`"French Content Standard B9.2.3.1"`). All eleven statements were
recovered from `french_CCP_B7-B9.pdf` and written (36 records); the statements agree with the tail
of the polluted values in 35 of the 36 cases, which is how we know the recovery is not inventing
anything. Note that the **served** copies (`data/curriculum/`) were already clean — this repaired
the unsent copy, so nothing a teacher sees changed for B7–B9.

Two print defects came out of it, both reported rather than guessed at:

- **`B6.1.2.5.3`** — the print numbers a *fifth* content standard (p93) in a sub-strand whose scope
  table lists four, so no skill can be named. Its `cs_desc` stays empty, and a test pins that it is
  the only one.
- **`B9.2.3.1`** — the print labels the content standard `B8/JHS2.2.3.1` inside the B9 section
  (p102). The statement ("Comprendre et s'exprimer sur son pays") was recovered by anchoring on the
  indicator row rather than on the code, and the discrepancy is in the audit artefact.

B4–B6 are served from `data/reference/`, so the fill reaches teachers: `public/curriculum` carries
all 266 descriptions, and four contract tests in `src/curriculumBundle.test.js` hold the rule, the
one deliberate blank, the bundle, and the absence of neighbour-column text.

## P2 — engineering debt that makes every future fix expensive

> **P2-2 (CI workflow) is done** — `.github/workflows/ci.yml` runs `make check` plus a
> stale-inventory guard on every push. Its number is left empty rather than renumbering,
> because other items reference these ids.

| # | Item | Why | Size |
|---|------|-----|------|
| P2-1 | ~~**Firestore rules unit tests** with the emulator, and smoke tests for the exporters~~ — **done 2026-09-19**: the rules are tested against the emulator in `tests/rules/firestore.rules.test.js` (`yarn test:rules`, and the `rules` job in CI), the exporters in `src/lib/pdfExport.test.js` and `src/lib/docxExport.test.js` (`lessonPlanDocx`, `schemePdf`, …) | Rules are the real security surface (see `docs/analysis/FINAL ANALYSIS.md`). The **Word** exporters are no longer untested — `src/lib/docxExport.test.js` unzips the generated `.docx` and checks the parts Word obeys (styles resolve, fonts declared, A4, footer present); the PDF/PPTX exporters and the emulator half of this item remain open. `src/lib` helpers, `buildTree`, the `public/curriculum` bundle contracts **and the rules invariants** are now covered by `yarn test` — but those are static: they prove a guard was not deleted, not that a permission decision is right. Only the emulator can do that. **Update 2026-09-19**: the exporter half is done — `src/lib/pdfExport.test.js` builds every PDF (`buildSchemePdf`, `buildLessonPlanPdf`, `buildQuestionPaper`) and both decks (`buildLessonSlidesPptx`, `buildQuizPptx`) and asserts what a reader obeys: the file starts `%PDF-` and ends `%%EOF`, the scheme is landscape A4 (842×595) and the plan portrait (595×842), the marking scheme earns its extra page only when answers are included, an empty selection still yields one page instead of throwing, and each deck is a real OOXML package whose `presentation.xml` registers every slide part and whose slides still carry the lesson, question and answer text. That required splitting each exporter into a `build…` function returning the document and a `download…` wrapper that saves it — which immediately caught a live bug: the new `downloadQuestionPaper` wrapper was forwarding only three options, dropping the `totalMarks` both callers pass. **The emulator half is done as well.** `tests/rules/firestore.rules.test.js` is a permission matrix put to a real rules engine, not to a reader: a signed-in caller with no profile document is denied everywhere; a signup cannot write itself `approved` or `admin`; a school admin can move a member into *their* school and not another; a like cannot inflate its tally, count somebody else, or smuggle in a third field; a private draft is invisible to a colleague while the same colleague reads a `members` row; the author-scoped and visibility-scoped lists the client runs are allowed and an un-filtered list of `notes` or `generated_documents` is **denied** (the production bug this rule shape was rebuilt for, P2-7); a library record whose `storagePath` points at another member's folder is refused; `quote_likes` can only move ±1 and only for the caller; a pending author still reads their own draft; the public pages stay readable without a token. `tests/rules/storage.rules.test.js` does the same for the file itself (an upload only under your own uid, the 8 MB cap, reads owner-or-admin, `generated/` the only writable tree) — 97 checks in all, 85 Firestore + 12 Storage, all green in CI. It is deliberately **not** in `make check`: the emulators are JVM programs, and `yarn test:rules` downloads a pinned firebase-tools plus the emulator jars on its first run. CI runs it in its own `rules` job (Temurin 21), which is the one place a permission *decision* — rather than the absence of a guard — is tested before a deploy. **It paid for itself on the first run**: 84 of 85 checks passed; the one that failed was `generated_documents`, where create demanded `storagePath.matches('^generated/<uid>/')` — a prefix pattern against a full-match engine, so the document library would have refused every save the moment the new rules were published. The pattern now ends with `.*` (`firestore.rules`, and the trap is written up in gotchas.md) | M |
| P2-3 | ~~**Bundle strategy**: chunk the 39.7 MB bundle per grade+subject and key the service-worker cache off a content hash~~ — **done 2026-09-19**: schedules now ship as **one file per subject-grade** (`schedules/b9-mathematics.json`, ~0.5 MB, 73 files) instead of one 3.5–4.8 MB array per grade, and `useSchedules(grade, subjectId)` fetches exactly the one it needs — the 4.8 MB fetch behind a B9 scheme is now ~0.5 MB. The only screen that wants a whole grade (`TermCalendar`) uses `useGradeSchedules()`, which loads the subject files in parallel and appends them as they land. `build_app_curriculum.py` hashes every payload it writes into `_BUILD_REPORT.json` as `bundleHash`, and `public/sw.js` names its cache after it — a rebuilt bundle invalidates itself on activate because it is fetched with a cache-busting query the outgoing worker has never cached. Tests hold the file names, the lesson count (13,140) and the report/hash contract | `b9_schedules.json` alone was 4.85 MB, and a rebuilt bundle reached returning users only after a manual `sw.js` edit — two silent-failure modes: a slow planner on a school connection, and stale curriculum on a device that had already installed the app | M |
| P2-4 | ~~**Pagination** for the capped lists with `startAfter` cursors~~ — **done 2026-09-19**: `usePagedCollection` (in `src/hooks/useCollection.js`) keeps the live first page and appends older ones with a `startAfter` cursor, and `hasMore`/`LoadMore` make "that is everything" a statement the page can actually make. Wired into the member libraries — lesson plans, schemes, notes (24/page), question bank (50/page, `QuestionBank` + `QuizMaker`) — and the paged queries are ordered server-side, so a page means the newest N rather than N arbitrary documents. The capped *previews* (`Workspace` contributions, `AuthorPage`, `Members`, `Articles`, `Vacancies`, `SlideLessons`) now pass `ordered: true`, which is what makes "20" mean "the 20 most recent" (it needed four new composite indexes, all added). `Search` still reads at most 100 per source and is documented as a best-match list, not a library | Silent truncation is indistinguishable from "no more data", and the caps were worse than that: without `orderBy` Firestore returns documents in internal-id order, so a busy collection hid *arbitrary* rows and the client-side sort only reordered whatever happened to arrive | M |
| P2-5 | ~~**Slides**: browse + PPTX export exist; there is no authoring form and no deck-view page~~ — **done 2026-09-19**: finished rather than removed. `/portal/slides/:deckId` (`SlideLessonView.jsx`) shows a saved deck slide by slide, exports it, and hosts the comment thread the save button had always promised (the rules already allowed `lesson_slides/{id}/comments`); `/portal/slides/:deckId/edit` (`SlideLessonForm.jsx`) edits grade/subject/term/week and each slide — title, indicator, objectives, activities, key words, closing question — with add/remove/reorder, writing back the arrays the PPTX exporter reads. Both pages sit behind the existing route, which stays hidden from the side panel per the earlier decision | Half-built feature — the save button promised a deck page that did not exist | M |
| P2-7 | ~~**Rework note/plan visibility into queries**~~ — **done 2026-09-19**: the read rules for `notes`, `lesson_plans` and `weekly_forecasts` gate on the document again — `isApprovedOrAdmin() && (isAdmin() \|\| isOwner(authorId) \|\| visibility in ['members','public'])` — and every list of those collections is scoped to a query the rule can prove: the Mine tab (`authorId == uid`) and the Shared tab (`visibility in [...]`), including the two queries `Search.jsx` now runs per gated collection. Each form offers **"Only me (draft)"** (`visibility: 'private'`), so private drafts are real again. Two deliberate limits, both documented: `status` stays a label because notes written before it existed have no such field (a status-gated list would hide them — `NoteForm` writes it now for a future backfill), and `lesson_slides` keeps a document-independent rule because its list is still un-filtered. **Deploy order matters**: publish the rules after the new build is live, and deploy the indexes with them — the test in `src/firestoreRules.test.js` scans both hooks so a screen cannot quietly go back to an un-filtered list | Restores per-document privacy without breaking list queries | M |
| P2-9 | ~~**Audit C2 wants 8 generated `Basic1_*_Lesson_Plans_Full_Year.docx` files**~~ — **done 2026-09-18**: those documents are build outputs generated from the enriched JSONs and are *not* in the repository (`.gitignore` covers `dist/books/` and `books/`), so `audit_c_lessons.py` now reports an absent document as `MISSING` — "not built here" — instead of `FAIL`, prints the tally (`0 of 8 present — 8 not built here`), and the refreshed `audit_c_results.json` records that state with a `summary` block instead of the stale PASS rows from a machine that had them. C1, the JSON checks, is unaffected and is what `make check` stands on | An audit that recorded PASS for files the repo does not hold | S |
| P2-8 | ~~**~150 legacy scripts still import `_compat` from a `tools/` directory that no longer exists**~~ — **done 2026-09-19**: 130 scripts pointed at `parents[2] / "tools"`, 33 used `Path` without importing it, and `scripts/build_app_curriculum.py`, `fix_grade_band_labels.py` and `package_books.py` used `ROOT / "tools"`. All of them now put `scripts/` on the path (the three deeper ones under `scripts/build/lessons/` at the right depth), the stale `tools/` references in docstrings are gone, and **`scripts/check_scripts.py`** — wired into `make check` and CI — fails on a script that does not compile, imports `_compat` without putting it on the path, or uses `Path`/`sys` without importing it. 201 scripts checked, 0 problems; the legacy generators run again (`build_math_word_document.py` wrote its Word file). **Updated 2026-09-21**: the repair had missed the Makefile itself — `make boot-check` and `make list-modules` still called `tools/ncosctl.py`, which moved to `legacy/tools/` with the retired kernel app (and fails there too: no `ncos` package outside it). The two targets are gone; `make audit`'s LEGACY block is the kernel-vs-dataset reference | The three `scripts/audit/` scripts were repaired on 2026-09-18 so the audits could run again. The rest are one-shot generators, but they are the record of how each database was built, and a script that cannot import its own shim documents nothing | S |
| P2-6 | ~~**Repo hygiene**: decide whether the 38 MB curriculum bundle stays in git (or moves to a release artefact)~~ — **decided 2026-09-19: it stays, with guard rails**. The measurements and the reasoning are in *Does the bundle belong in git?* (`docs/curriculum-data.md`); the short version: Vercel deploys the repo, the app is a PWA whose files *are* the product, and the bundle is deterministic — `make bundle-check` rebuilds it from `data/` and requires an unchanged tree, so a commit only touches what actually changed. Added: `scripts/bundle_size_report.py` (`make bundle-size`, fails above `BUNDLE_BUDGET_MB`, default 80 MB), the rebuild check, `.gitattributes` marking the regenerated layers `linguist-generated` so GitHub collapses their diffs, and both checks in `make check` + CI. The stale `_BUILD_REPORT.json` total (38,638 KB — it predated the question bank) is corrected. Also done earlier: the 4 tracked `.pyc` files were untracked on 2026-09-18 | Keeps clones and diffs manageable | S |

## P3 — product bets, once the P0s are done

| # | Item | Notes |
|---|------|-------|
| P3-1 | ~~**Publish the B1 Mathematics pilot books** and roll the generator out subject by subject~~ — **done 2026-09-19**: `scripts/build_books_rollout.py` drives `seed/build_book_skeleton.py` across the **served** subject-grades (so books keep step with the curriculum the portal contains), reports each one built/present/failed, and keeps `data/books_manifest.json` — `books/` is gitignored, so the manifest is what the repository records: 84 subject-grades, **168 documents (7.7 MB)**, structure counts per book and a content hash of each. The full rollout built 83 and found the pilot already present; 0 failures, kindergarten included (no schedule — seeded from indicator text). The pilot publishes as one file: `make books-publish SUBJECT=mathematics GRADE=B1` → `dist/books/mathematics-B1.zip` (textbook + workbook). `src/booksManifest.test.js` holds the manifest to the served bundle and to the generator's contract — **one lesson per indicator**, true for all 84. The generator gained `--out` (write/measure anywhere) | `books/` is gitignored (regenerable, never overwritten) | M |
| P3-2 | ~~**Offline packs** — per subject/class downloads for schools without reliable connectivity~~ — **done 2026-09-19**: a **“Save for offline”** button on every subject page (`SubjectBrowser.jsx`) fetches that subject-grade's five bundle files (`grades.json`, the grade's subjects/indicators/schemes, and `schedules/<grade>-<subject>.json`) through the service worker and reports progress file by file and the size when it lands. `src/lib/offlinePack.js` holds the file list and the fetch loop — deliberately tiny, because the caching is the worker's job: the pack puts the same entries in the same cache that browsing would, so there is no second copy to invalidate. Files are fetched with `cache: 'reload'`, so a pack never silently keeps a stale copy, and a missing file is reported rather than failing the pack (a subject with no schedule is normal) | The prerequisite landed with P2-3: the served unit *is* the subject-grade. Without an explicit affordance, "will this work in a classroom with no data" was left to whether the teacher happened to have opened the right pages |
| P3-3 | ~~**Store generated documents, not just metadata** — history has no re-download because nothing writes to Storage~~ — **done 2026-09-19**: the file itself now goes to Cloud Storage under `generated/{uid}/{timestamp}-{name}` with a record in `generated_documents`, and a **My library** page (`/portal/library`) can open it again or delete both halves. Access is `storage.rules`' job: writes only inside the caller's own uid folder, reads owner-or-admin, an 8 MB cap in the rule, everything outside `generated/` denied including reads; `firestore.rules` adds an owner-scoped read (so the library's list is filtered by `authorId`) and refuses a create whose `storagePath` is not inside the caller's own folder. Every page that downloads a document now offers `<SaveToLibrary>`, and a test fails if a new page forgets — the build runs lazily, only when pressed. The upload happens **before** the record, so a failed upload leaves no broken entry. Deploy is separate: `make deploy-storage` (or `firebase deploy --only storage`); the emulator needs a JVM, so `src/storageRules.test.js` is the guard that runs on every check | Needs a `storage.rules` (see the paid-delivery work in `docs/deliveries-roadmap.md`) | M |
| P3-4 | ~~**Visualisation engine** — 3–5 interactive models on one subject-grade as a pilot, not 50~~ — **done 2026-09-19**: **five** models for **mathematics B4** at `/portal/models` (also linked from that subject's curriculum page): place value frame, compare/order/round on a number line, fraction strips, factor arrays, and perimeter & area. Each one is a real control surface — inputs that change what the pupils see — and names the served indicators it teaches, so the screen is never divorced from the syllabus. All local state and inline SVG/CSS: offline, no service, no key. `src/lib/teachingModels.js` is the catalogue, `src/components/models/` the code, and `src/teachingModels.test.js` holds them together — every model must name indicators that are served for its own subject and grade, every component must exist, and the pilot must stay between three and five models. **Deliberately one subject-grade**: the point is to find out whether a screen helps in a B4 classroom before building fifty | Needs P2-3 to be realistic in classrooms (done) | M |
| P3-5 | ~~**Study-notes export** (PDF/DOCX)~~ — **done 2026-09-19**: `src/lib/htmlBlocks.js` reads the note's Tiptap HTML into headings/paragraphs/lists (shared by both exporters, so Word and PDF agree), `noteDocx.js` writes the Word document in the same house style as the lesson plan (title block, meta, reference table, branded footer) and `notePdf.js` flows the same blocks onto A4 pages. Both are on the note page next to Edit. 11 tests cover the reader (inline markup, `<br>`, entities, malformed and empty HTML) and both files (real PDF, heading styles that exist, a long note flowing onto more pages) | Notes are network-only — a teacher cannot hand one to a colleague or pin it in class |

---

## Deferred by decision

| Item | Why not | Revisit when |
|---|---|---|
| **Server-side hard gate** for paid downloads (moves final generation server-side so a clean file cannot exist before approval) | Needs Cloud Functions; Spark plan does not allow them | Agents are selling consistently, or bypassing the soft gate becomes a real risk |
| **Mobile Money automation** (gateway webhook → `paid`) | Same infrastructure blocker; manual "Mark as paid" collects MoMo today at ₵0 | Volume makes the manual loop a bottleneck |
| **Ad-hoc quiz/question-paper delivery** | They are generated from a selection with no stored doc, so the approve/regenerate flow has nothing to re-render; persist the artefact first | P1-5 gives the bank content worth gating |
| Building the Studio as a separate web app | One author; `scripts/` + git already is the Studio | A second content author joins |
| Selling printed books as the primary channel | The owner is replacing the consortium print model with a self-serve portal | — |
| Porting the Python generators to JavaScript | They work; the browser needs the *same content model*, not the same language | The pipeline becomes a runtime dependency |
| Regenerating the 73 books before every demo | The CI workflow exists, but does not build books yet: if the books matter, build them in the pipeline, not by hand | — |

---

## Numbers referenced by this file

All from `make audit` (`data/inventory.json`). If an item above disagrees with the
inventory, the inventory is right and the item is stale — fix the item.
