# Build output
dist
dist-ssr

# Dependencies
node_modules
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Env / secrets
.env
.env.local
.env.*.local

# Python pipeline
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
dist/books/
books/

# Editors / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Audit scratch: PDF text extracted by scripts/audit/audit_b_pdf_crosscheck.py,
# cached so repeat runs are fast. Regenerated on demand; never committed.
data/audit/pdf_text_cache/

# Same, for the per-chunk coordinates scripts/fix_reference_text.py needs to tell
# a table's columns apart.
data/audit/pdf_chunk_cache/
